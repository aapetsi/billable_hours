import React from 'react'
import Receipt from '../components/Receipt'

test('renders learn react link', () => {
  // const { getByText } = render(<App />)
  // const linkElement = getByText(/your/i)
  // expect(linkElement).toBeInTheDocument()
  expect(1).toBe(1)
})
